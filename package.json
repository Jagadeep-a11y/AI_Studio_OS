{
  "name": "ai-studio-os",
  "version": "0.2.0",
  "private": true,
  "description": "AI Studio OS — a creative AI workspace with a provider-agnostic generation gateway and SQLite persistence.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --no-warnings=ExperimentalWarning server/index.js",
    "dev": "node --watch --no-warnings=ExperimentalWarning server/index.js",
    "test": "node --no-warnings=ExperimentalWarning server/selftest.js"
  },
  "dependencies": {}
}
